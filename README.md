## Answers to Questions

### 1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
getElementById is use for only get the unique id, getElementsByClassName is use to get element by classname which use severel time in a j, querySelector is use for get element by diffrent types of element, querySelectorAll is also similer to querySelector but querySelector gives just first element where querySelectorAll gives all element.

### 2. How do you create and insert a new element into the DOM?

document.creatElement('element name')

### 3. What is Event Bubbling? And how does it work?
Event bubbling is recived an event and bubbling up to parent.
when clicked an event its bubbling to the parent.
### 4. What is Event Delegation in JavaScript? Why is it useful?
Event deligation is a technic to handle child element in a parent elment.
### 5. What is the difference between preventDefault() and stopPropagation() methods?
preventDefault() is a method to stop the default action of that the browser naturally performs. and a stopPropagation() method is stop prapagation in a bubbling event.
